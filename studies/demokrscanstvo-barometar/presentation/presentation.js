(()=>{'use strict';const slides=[...document.querySelectorAll('.slide')],mode=document.querySelector('#mode'),prev=document.querySelector('#prev'),next=document.querySelector('#next'),position=document.querySelector('#position');let active=false,index=0;
const fromHash=()=>Math.max(0,Math.min(slides.length-1,(Number(location.hash.replace('#slajd-',''))||1)-1));
function fit(){if(!active)return;const bar=document.querySelector('.toolbar').getBoundingClientRect().height;const scale=Math.min(innerWidth/1200,(innerHeight-bar)/675);slides[index].style.transform=`scale(${scale})`;slides[index].style.left=`${(innerWidth-1200*scale)/2}px`;slides[index].style.top=`${bar+(innerHeight-bar-675*scale)/2}px`;}
function show(i,focus=false){index=Math.max(0,Math.min(slides.length-1,i));slides.forEach((s,j)=>{s.classList.toggle('active',j===index);s.inert=active&&j!==index;if(!active){s.style.transform='';s.style.left='';s.style.top='';}});position.textContent=active?`${index+1} / ${slides.length}`:'';prev.disabled=index===0;next.disabled=index===slides.length-1;if(active){history.replaceState(null,'',`#slajd-${index+1}`);fit();if(focus)slides[index].focus({preventScroll:true});}}
function toggle(){active=!active;document.body.classList.toggle('present',active);mode.textContent=active?'Čitaj sve slajdove':'Prikaži slajdove';mode.setAttribute('aria-pressed',String(active));prev.hidden=next.hidden=!active;show(active?fromHash():index);if(!active)slides[index].scrollIntoView();}
mode.hidden=false;mode.setAttribute('aria-pressed','false');mode.addEventListener('click',toggle);prev.addEventListener('click',()=>show(index-1));next.addEventListener('click',()=>show(index+1));addEventListener('resize',fit);addEventListener('hashchange',()=>{if(active)show(fromHash());});addEventListener('keydown',e=>{if(!active||e.altKey||e.ctrlKey||e.metaKey||/INPUT|SELECT|TEXTAREA/.test(e.target.tagName))return;if(e.key==='Escape'){toggle();mode.focus();}else if(['ArrowRight','PageDown','ArrowLeft','PageUp','Home','End'].includes(e.key)){e.preventDefault();show(e.key==='Home'?0:e.key==='End'?slides.length-1:index+(['ArrowRight','PageDown'].includes(e.key)?1:-1),true);}});show(fromHash());
})();
