# Revision 3 execution checkpoint

This report records implementation evidence, not an empirical publication.
The authoritative decisions and qualifications remain in the single execution
plan. The PI authorized recommended decisions and gate actions once supported.

## Gates reached

- G1: frozen panel of 115 outlets, with source snapshot and panel hashes.
- G2: definition `1.0.0+33429974d0b8`, 105 entries, independently reviewed
  development evidence for 217 cases. The frozen evidence hashes still match.
- Full private classification completed in all 69 monthly chunks: 1,075,321
  candidate decisions and 21,968,320 evidence records. Frozen input, engine,
  evidence and classifier-body checks pass.
- The blinded private coding package is prepared and verified: 320 unique PI
  items, with 80 assigned for independent double coding. No human labels were created.
- Both June 2024 source reconstructions completed read-only. The provisional
  diagnostics pass the numerical bridge criteria, but use unvalidated routes.
  The policy remains `not_comparable_seam` until actual human route acceptance.
- G3–G6 remain pending. No public aggregate installation, production integration,
  PR acceptance, merge, deployment, L1, L2 or L3 is claimed.

## Verification

The combined R runner passed all 69 shared DigiKat checks and all barometer
suites. Added recovery/refresh checks include 20 worker, 48 release, 21 history,
13 certificate, 56 installer, 29 update, 27 publication-policy and 31 CLI cases,
using invented temporary inputs. A separate independent CLI review passed 22
checks, including a database-connection sentinel: pending G3 stops the default
refresh before any source connection.
The synthetic source-to-release pipeline passed on 3,206 invented records with
the real 49-column source schema. The empirical DuckDB version stays 1.5.4;
CI's invented checks also passed against the checksummed 1.5.5 Windows binary.

The static/enhanced matrix regression and six JavaScript tests pass. The
independent numeric review checked all 4,900 available synthetic theme cells,
all 84 static matrix cells and the SVG/PNG exports. The site reviewer checked
16 page cases plus all eight standard widths, with no axe, overflow or browser
failures and working download links. Corrected export caption contrast is 6.65:1.
The final expanded page is 1,337,926 bytes with a 182,665-byte embedded payload.
Sixteen browser cases plus nine expanded-content cases pass, along with all
eight standard widths, 46 additional numeric checks and 73 local links.
Normal direct printing with JavaScript active passes for monthly/broad and
weekly/narrow; raster inspection confirms the full matrix, tables and charts.
Print uses solid unavailable bands to avoid a Chromium SVG-pattern PDF hang,
and exposes closed details tables while keeping cards intact. A separate
artificial print-media-to-screen toggle still triggers a Chrome 153 timeout;
the passing artifacts use normal direct printing.

The repository source-quality contract passed 391 checks. Disclosure screening
passed 257 trackable study/public-data artifacts. The site link checker passed
137 HTML files after two pre-existing overview links were corrected to their
tracked PNG targets. That fix is commit `9ae51cb`; its source, freeze cache and
rendered HTML were verified separately. Tracked `data/processed/` files did not
change.

The Croatian method-only conference PDF is two pages, rendered and visually
checked. It contains methodology and status, no empirical indicator findings.
The coding template has a Croatian guide and readable labels while preserving
the import schema. Syntax and field-map checks pass. Its headless browser check
was blocked by automatic approval review (`blocked by policy`), and the
supported browser tool reported no available browser. That particular native
form check is not counted as passed.

## Boundaries and next work

Synthetic generated HTML, downloads and the synthetic sitemap entry are not
release artifacts and are excluded from staging. Private article material,
development diagnostics, source caches and future human answers stay ignored
and outside the public data path. Both source HTML templates are explicitly
trackable.

The prepared PI/second-coder package now awaits genuine independent human
coding and PI adjudication. See the private handoff below.
Vendor aggregate-licence evidence is also outstanding. The empirical edition
PDF, dated empirical findings, production navigation, publication governance
and final full-site release checks follow G3/G4. The page's method details and
research metadata are prepared and checked on synthetic data.

After classification exited successfully, `run.R` was connected to the reviewed
refresh helper. With no arguments it runs a checked update with eight workers;
`--edition` explicitly requests a new monthly edition, and
`--stage=accept-validation` records G3 only from actual completed evidence.

## Fresh-checkout follow-up

Checkpoint `e892f83` and the final overview-dimension fix `3fdb058` are committed.
A detached checkout at `3fdb058` reproduced all 40 generated synthetic public
files byte for byte, including CSV, JSON, SVG, PNG and manifests. Only the
explicitly exempt `computed_at` value was held at a fixed test time. Both trees
used the same locked R library; the test unset the external-source variables.

The final page/export checkpoint `5da8aa4` also reproduced all 40 synthetic
public files byte for byte in a fresh detached checkout with the same timestamp
exemption. Subsequent changes are limited to CLI routing, its tests and handoff
documentation/screenshots; the release-generation code is unchanged.

The detached checkout also completed the full 115-source Quarto render. After
restoring the nine frozen study pages and their tracked libraries, its link
check passed 136 HTML files and its metadata/structure/budget check passed 126
active pages. Tracked processed data stayed unchanged. This verifies the source
checkpoint's ordinary site build; the empirical barometer remains excluded,
and it is not the G5 production integration or a deployment.

## D19 evidence search

No licence/contract file was found by a filename search in the supplied
DetermDB directory. The public [Determ terms](https://determ.com/tos/), reviewed
on 18 September 2026 and marked updated 19 August 2024, restrict service/content
reuse and do not expressly grant a CC BY licence for derived aggregates.
Accordingly, I cannot use that page alone as evidence satisfying D19. The
project-specific agreement or written confirmation remains needed. No vendor
was contacted.

## Page and release-policy follow-up

The page now reads manifest-checked dictionary, panel, validation and history
files and the hash-bound dated edition. It displays denominator-only outlet
tables, pending human-validation status, family summaries, release history and
separate A2 diagnostics in the narrow view. Related-study navigation, Dataset
metadata, citation links and the methodology continuity anchor are prepared.
The domain review clarified B/C application, C's strength threshold, date-valid
D attribution and the fact that an A2-labelled article can qualify separately
through B/C. These are synthetic page preparations, not empirical findings.

Independent numeric review passed all 734 primary period rows, 367 A2 keyed
rows, 350 available A2 SVG points, 50 families/105 entries and 72 invented
outlet-year denominator cells. The precision/κ formatter now preserves small
proportions instead of applying the headline-rate display threshold. The full
R suite passed after the edition/diagnostic reader changes; the update fixture
was brought into line with the stricter production manifest requirement.

An independent release review reproduced a policy gap: B/C could satisfy the
frozen statistical scorer when A1 was dropped, experimental or unvalidated.
The adopted plan forbids a release in all three cases. A separate publication
policy now enforces accepted A1 in scoring import, G3 acceptance, aggregation,
refresh and installation, and binds its code hash to the certificate. The
frozen scorer and all G2 evidence remain unchanged. All 27 independent policy
regressions, 13 certificate checks and 29 update checks pass on invented data.

The eight-token overlap scan now caches public n-grams and vectorizes the same
consecutive-token joins. Independent scalar-reference tests confirmed identical
matches, including Unicode, punctuation, missing/short strings and several n.
The obsolete scans were superseded by a final partitioned scan of all private
candidate chunks, each using the same checker and isolated output directory.
The completed final scan is recorded below.

The final partitioned scan subsequently completed successfully against all
69 private candidate Parquet chunks. It checked the synthetic public exports,
page source, JavaScript wording, conference method source and the new shared
methodology section. Every partition used the same exact eight-token checker;
public input hashes stayed unchanged during the run. No restricted passage was
printed. This certifies the reviewed synthetic/method wording only, not an
empirical release or its future findings.

## Verified private coding handoff

The immutable package lives under
`WORKDIR/validation/1.0.0+33429974d0b8/`. Open `human_PI.html` locally for the PI
and give `human_second.html` to the independent second human. Each enters their
own name and exports their own answers; neither sees the other's answers before
independent coding finishes. Follow [PIPELINE.md](../studies/demokrscanstvo-barometar/PIPELINE.md)
for import, PI adjudication, bridge reconstruction and G3 acceptance commands.

The full frozen sampling design was used. A1 has 60 sampled items, A2 and A?
have 30 each, near misses have 60 and recall probes have 100. B, C and D have
fewer available held-out cases than their targets, so their full eligible
strata were censused: 24, 13 and 3 respectively. This produces 320 distinct
coding items and the required 80 assigned for double coding; it is not a switch to the
reduced design. Sparse B/C evidence will be reported transparently when actual
human estimates and intervals become available.

Both package HTML payloads contain only the prescribed blinded item fields.
Their item IDs/order, nonempty contexts, role, version and draw identity match
the frozen assignments. The package manifest and current sampling/context/
template hashes pass. Development items and logged exclusion cases are absent.
No evaluation passage was displayed to the assistant. No answers, adjudication
or validation-result file has been generated. Private audit receipts are
`validation/preparation-audit.json` and `validation/coding-package-audit.json`.

The classification recipe records the original extraction-file hash used by
the completed run. A parsed-function comparison confirms that the classifier
body remained identical after the scheduling wrapper improvements. All eight
G2 evidence hashes still match. The draw remains bound to that completed
classification, its input snapshot, panel, engine and boilerplate identity.

The [readiness checklist](2026-09-18_barometar-readiness-checklist.md) maps brief
requirements to current evidence and outstanding work. Twenty-four reviewed
synthetic screenshots are retained under
`screenshots/2026-09-18-barometar-synthetic/`; no empirical release is represented.
