# Themes and vocabulary in the conference carousel

Use the author's preferred examples to reshape slides 5–8 into the complete
thematic distribution, frequent words, five selected word pairs, and a yearly
theme heatmap. Retain 18 slides, the six data exploration slides and the approved
navy/gold design. Use “pojam demokršćanstva”, never “demokršćansko ime”.

Read existing public theme tables and the published lexical report. Preserve
their distinct denominators: 2,306 publications for themes, 2,253 distinct texts
for lexical frequencies. Keep word pairs as selected qualitative associations,
without invented frequencies or causal implications. Explain heatmap shares
within each year and the collection change/partial final year.

Generate from source, inspect changed slides, run browser/PDF checks and the
focused landing-page render. Previous automatic approval review prohibited a
full-site render because unrelated dirty work would be overwritten. Preserve
that work and use the approved focused route. Commit/push the scoped revision
under the standing publication authorization, then verify the live assets.

## Results

Slides 5–8 now show all ten topics, the published 48-word cloud with its three
leading counts, five selected word pairs, and the ten-topic/six-year heatmap.
The hosting page uses the same corrected terminology. The 18-slide structure,
conference palette and six exploratory slides remain intact.

Rendered and visually reviewed the PDF. Its numeric, font, geometry and link
checks pass. Carousel browser checks report zero accessibility violations and
pass keyboard navigation plus all four mobile widths. The focused Quarto render
passes, as do static checks on 127 pages and browser checks on the hosting page
at eight widths. Public/source asset copies match byte for byte. No corpus or
existing aggregate data changed.
The site link check also passes for 140 HTML files, with no missing local targets,
anchors or encoding defects.
