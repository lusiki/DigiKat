---
title: "Tematske struje — anatomija diskursa katoličkog digitalnog prostora u Hrvatskoj (2021.–2026.)"
subtitle: "Tematske struje u stratificiranom uzorku korpusa"
date: last-modified
categories: ["Tematske struje", "NLP", "16 kategorija"]
---

```{r}
#| label: setup
#| include: false

# Globalne postavke za dokument
knitr::opts_chunk$set(
  echo = FALSE,
  message = FALSE,
  warning = FALSE,
  fig.width = 14,
  fig.height = 11,
  dpi = 200,
  # Cream device canvas so the base-graphics wordcloud (not a ggplot) also matches
  # the page background; ggplots already fill plot.background via theme_digikat().
  dev.args = list(bg = "#F5F4F0")
)
```

```{r}
#| label: libraries
#| include: false

# Učitavanje paketa potrebnih samo za prikaz već validiranih sažetaka
library(scales)
library(dplyr)
library(tidytext)
library(stringr)
library(ggplot2)
source("../../R/theme_digikat.R")
library(forcats)
library(ggridges)
library(ggraph)
library(igraph)
library(wordcloud)
```

```{r}
#| label: data-preparation
#| include: false

# Teški izračuni nad stratificiranim uzorkom izvode se jednom skriptom
# R/05_page_summaries.R. Stranica čita samo provjeren, agregiran ulaz za grafikone.
source("../../R/lib/page_summaries.R", encoding = "UTF-8")
digikat_load_page_summary(
  "../../data/page-ready/mapa_stats.rds",
  "mapa_stats",
  envir = environment()
)

# Opseg korpusa dolazi iz manifesta službene baze, a ne iz utipkane brojke. Provjera agregata
# prekida render ako priprema u data/page-ready/ ne potječe iz aktualnog korpusa.
source("../../R/lib/digikat_paths.R", encoding = "UTF-8")
mf <- digikat_page_corpus("../..")
n_objava <- digikat_hr_int(mf$corpus$rows)
raspon   <- paste0(mf$corpus$year_min, ".–", mf$corpus$year_max, ".")
nlp_mf <- jsonlite::fromJSON("../../data/nlp/manifest.json", simplifyVector = TRUE)
sample_n <- nlp_mf$pages$mapa_stats$sample_rows
sample_target <- 100 * nlp_mf$parameters$page_proportions$mapa_stats

fmt_list <- function(x) {
  x <- as.character(x)
  if (length(x) == 1) return(x)
  paste0(paste(head(x, -1), collapse = ", "), " i ", tail(x, 1))
}
fmt_topics <- function(x) fmt_list(digikat_topic_label(x))

top_word_names <- word_freq_for_cloud %>%
  arrange(desc(n)) %>%
  slice_head(n = 9) %>%
  pull(lemma) %>%
  fmt_list()

top_topic_names <- topic_trends_guided %>%
  group_by(topic) %>%
  summarise(total = sum(total_mentions), .groups = "drop") %>%
  arrange(desc(total)) %>%
  slice_head(n = 3) %>%
  pull(topic) %>%
  fmt_topics()

topic_intensity_medians <- thematic_intensity_data %>%
  group_by(topic) %>%
  summarise(median_intensity = median(intensity), .groups = "drop") %>%
  arrange(desc(median_intensity))
high_intensity_names <- fmt_topics(head(topic_intensity_medians$topic, 3))
low_intensity_names <- fmt_topics(tail(topic_intensity_medians$topic, 3))

top_engagement_names <- engagement_by_topic %>%
  arrange(desc(avg_interactions)) %>%
  slice_head(n = 3) %>%
  pull(dominant_topic) %>%
  fmt_topics()
top_love_names <- polarization_by_topic %>%
  arrange(desc(love_ratio)) %>%
  slice_head(n = 3) %>%
  pull(dominant_topic) %>%
  fmt_topics()
top_angry_names <- polarization_by_topic %>%
  arrange(desc(angry_ratio)) %>%
  slice_head(n = 3) %>%
  pull(dominant_topic) %>%
  fmt_topics()

pair_labels <- topic_pairs %>%
  arrange(desc(correlation)) %>%
  slice_head(n = 3) %>%
  transmute(label = paste(digikat_topic_label(item1), digikat_topic_label(item2), sep = " – ")) %>%
  pull(label) %>%
  fmt_list()
```

## Uvod u analizu sadržaja diskursa

Nakon pregleda glavnih aktera i platformi u [Mapi ekosustava](mapa.html), ova analitička mapa — **Tematske struje** — usmjerava fokus na sadržaj digitalnog diskursa. Polazi od korpusa s `r n_objava` objava u razdoblju `r raspon`, a obradu prirodnog jezika provodi na `r digikat_hr_int(sample_n)` dokumenata. To je ciljani `r format(sample_target, decimal.mark = ",")`-postotni stratificirani uzorak po godini i platformi među objavama s više od 100 znakova, bez TikToka. Godina 2026. nepotpuna je, pa usporedbe po godinama uključuju djelomično razdoblje.

Cilj mape jest dati pregled sadržaja u tri dijela. Prvi prikazuje učestalost i gustoću unaprijed definiranih tematskih rječnika. Drugi uspoređuje zabilježene interakcije te Facebookove reakcije Love i Angry među dominantnim temama. Treći prikazuje najčešće lematizirane vlastite imenice i supojavljivanje tematskih oznaka. `r nrow(engagement_by_topic)` kategorija definirano je teološkom i komunikološkom ekspertizom; riječ je o vođenom rječničkom označavanju, a ne o neovisno validiranoj klasifikaciji cjelokupnog značenja teksta.

<div class="metric-grid">
<div class="metric-card">
<div class="metric-value">`r nrow(engagement_by_topic)`</div>
<div class="metric-label">Tematskih kategorija</div>
</div>
<div class="metric-card">
<div class="metric-value">`r digikat_hr_int(sample_n)`</div>
<div class="metric-label">Dokumenata u uzorku</div>
</div>
<div class="metric-card">
<div class="metric-value">`r n_objava`</div>
<div class="metric-label">Medijskih objava</div>
</div>
<div class="metric-card">
<div class="metric-value">`r raspon`</div>
<div class="metric-label">Razdoblje analize</div>
</div>
</div>

<p>
  <span class="tag tag-primary">R</span>
  <span class="tag tag-primary">UDPipe HR 2.5</span>
  <span class="tag">NLP</span>
  <span class="tag">Lematizacija</span>
  <span class="tag">Tematski rječnici</span>
  <span class="tag">Mrežna analiza</span>
</p>

## Leksička struktura uzorka

Prije tematskog označavanja, oblak prikazuje do 100 najčešćih lematiziranih imenica, pridjeva i glagola u analiziranom uzorku, uz minimalnu frekvenciju zadanu u grafikonu. Ne prikazuje sve `r n_objava` objave. Prema novom uzorku prednjače riječi `r top_word_names`. Veličina riječi prikazuje frekvenciju, ali sama po sebi ne razlikuje temu, stav ni važnost riječi unutar pojedinoga dokumenta.

```{r}
#| label: plot-wordcloud
#| fig-height: 11
#| fig-width: 14

wordcloud(
  words = word_freq_for_cloud$lemma,
  freq = word_freq_for_cloud$n,
  min.freq = 50,
  max.words = 100,
  scale = c(7, 1.5),
  random.order = FALSE,
  rot.per = 0.30,
  colors = dk_palette
)
```

Oblak zato služi kao leksička orijentacija. Česte crkvene, društvene i vremenske riječi pokazuju širinu rječnika uzorka, ali za tvrdnje o narativima i odnosima među temama potrebne su strukturirane analize koje slijede.

---

## Struktura tematskog pejzaža

Prvi dio tematske analize usmjeren je prema razumijevanju opće strukture diskursa. U tom cilju odgovaramo na sljedeća pitanja: Koje tematske kategorije prevladavaju, a koje zauzimaju marginalne pozicije? Kako se njihova zastupljenost mijenja kroz promatrano razdoblje? Koliko su te teme centralne za članke u kojima se pojavljuju?

### Vremenska dinamika tematskih kategorija

Prema ukupnom broju rječničkih podudaranja u uzorku najzastupljenije su teme `r top_topic_names`. Grafikon prikazuje samo teme koje u barem jednom prikazanom presjeku prelaze 5 % spominjanja. Razlike među godinama opisne su i treba ih čitati zajedno s upozorenjem o promjeni prikupljanja.

::: {.callout-warning}
## Promjena metode prikupljanja (oko 2024.) — oprez pri čitanju vremenskih udjela

Korpus objedinjuje dva toka prikupljanja koji se smjenjuju u srpnju 2024., uz kratko tehničko preklapanje od 1. do 4. srpnja. Jedinstveno pravilo uključivanja primijenjeno je na oba toka, ali njihov početni obuhvat nije jednak. Dobavljačev zapis od veljače do svibnja 2024. nema tekst, pa u korpusu nema objava iz tih mjeseci. Budući da se mijenja i skup platformi, **promjene godišnjih udjela tema nisu nužno stvaran tematski pomak**. Vremensku dinamiku treba čitati uvjetovano tokom prikupljanja i platformom.
:::

```{r}
#| label: plot-topic-trends
#| fig-width: 14
#| fig-height: 11

ggplot(topic_trends_guided, aes(x = year, y = share, color = topic, group = topic)) +
  geom_line(linewidth = 1.5) +
  geom_point(size = 5) +
  scale_y_continuous(labels = scales::percent_format(decimal.mark = ",")) +
  scale_x_continuous(breaks = c(2021, 2022, 2023, 2024, 2025)) +
  scale_colour_digikat(labels = digikat_topic_label) +
  labs(title = "Promjena zastupljenosti glavnih tema (iznad 5% udjela)",
       x = "Godina", y = "Udio rječničkih podudaranja u uzorku", color = "Tema") +
  theme_digikat(base_size = 24) +
  theme(legend.position = "bottom")
```

### Intenzitet i centralnost tematskih kategorija

Frekvencija tema nije jedini relevantan indikator. Potrebno je razumjeti i centralnost teme za tekstove u kojima se pojavljuje. Ovaj grafikon prikazuje distribuciju intenziteta tematskih kategorija, mjerenog brojem tematskih spominjanja normaliziranih na tisuću riječi.

Najviši medijan intenziteta imaju `r high_intensity_names`, a najniži `r low_intensity_names`. Intenzitet je broj rječničkih podudaranja na tisuću riječi. Viša vrijednost znači gušći tematski rječnik, ali ne dokazuje da je tema „glavna vijest" niti da je niža vrijednost samo usputno spominjanje.

```{r}
#| label: plot-topic-intensity
#| fig-width: 14
#| fig-height: 11

ggplot(thematic_intensity_data, aes(x = intensity, y = fct_reorder(topic, intensity, .fun = median))) +
  geom_density_ridges(scale = 2, fill = dk_col$accent, color = dk_col$paper, alpha = 0.85) +
  scale_x_log10(breaks = c(1, 10, 100, 1000), labels = c("1", "10", "100", "1000")) +
  scale_y_discrete(labels = digikat_topic_label) +
  theme_digikat(base_size = 24) +
  labs(
    title = "Distribucija intenziteta tematskih kategorija",
    subtitle = "Koliko su teme centralne za tekstove u kojima se pojavljuju?",
    x = "Intenzitet teme (spominjanja na 1000 riječi, logaritamska skala)",
    y = "Tematska kategorija"
  ) +
  theme(panel.grid.major.y = element_blank())
```

---

## Zabilježene interakcije i reakcije prema temama

Drugi dio uspoređuje prosječan broj zabilježenih interakcija te udjele dviju Facebookovih reakcija među dominantnim temama. To su deskriptivni platformski signali, ne potpuna mjera recepcije publike.

### Zabilježene interakcije prema tematskim kategorijama

Najveći prosjek zabilježenih interakcija po dokumentu imaju `r top_engagement_names`. Poredak je deskriptivan: ne kontrolira platformu, veličinu izvora, godinu ni različite definicije interakcije.

Zato se iz grafikona ne može izdvojiti konflikt kao uzročni pokretač angažmana. On pokazuje koje se tematske oznake u ovom uzorku pojavljuju uz više interakcija, a objašnjenje razlike zahtijevalo bi model koji odvaja platformu, izvor i vrijeme.

```{r}
#| label: plot-engagement-by-topic
#| fig-width: 14
#| fig-height: 11

ggplot(engagement_by_topic, aes(x = avg_interactions, y = reorder(dominant_topic, avg_interactions))) +
  geom_col(fill = dk_col$accent, width=0.8) +
  geom_text(aes(label = round(avg_interactions)), hjust = -0.2, size = 6.5, color = dk_col$ink) +
  expand_limits(x = max(engagement_by_topic$avg_interactions) * 1.1) +
  scale_y_discrete(labels = digikat_topic_label) +
  theme_digikat(base_size = 24) +
  labs(title = "Zabilježene interakcije prema tematskim kategorijama",
       subtitle = "Prosječan broj interakcija po članku za svaku dominantnu temu",
       x = "Prosječne interakcije", y = "Dominantna tematska kategorija") +
  theme(panel.grid.major.y = element_blank())
```

### Udio reakcija Love i Angry prema tematskim kategorijama

Facebookove reakcije Love i Angry ovdje se prikazuju kao dva odvojena signala među dokumentima s više od deset ukupnih reakcija; grafikon zadržava teme s više od sto ukupnih reakcija u tom podskupu. One ne predstavljaju cjelovitu emocionalnu obojenost publike ni svih platformi.

Najveći udio reakcije Love imaju `r top_love_names`, a najveći udio reakcije Angry `r top_angry_names`. Ti omjeri opisuju zabilježene Facebookove reakcije uz dominantne teme; ne mjere izravno polarizaciju društva.

```{r}
#| label: plot-polarization-by-topic
#| fig-width: 14
#| fig-height: 11

ggplot(polarization_by_topic, aes(y = reorder(dominant_topic, angry_ratio))) +
  geom_col(aes(x = angry_ratio), fill = dk_col$neg, alpha = 0.85) +
  geom_col(aes(x = -love_ratio), fill = dk_col$pos, alpha = 0.85) +
  geom_vline(xintercept = 0, color = dk_col$faint) +
  scale_x_continuous(labels = function(x) percent(abs(x), decimal.mark = ","), name = "Udio u ukupnim reakcijama") +
  scale_y_discrete(labels = digikat_topic_label) +
  theme_digikat(base_size = 24) +
  labs(title = "Udio Facebookovih reakcija Love i Angry prema tematskim kategorijama",
       subtitle = "Plavo = udio Love reakcija | Crveno = udio Angry reakcija",
       y = "Dominantna tematska kategorija")
```

Love i Angry mogu imati različita značenja ovisno o sadržaju i publici. Bez analize komentara i konteksta nije opravdano poistovjetiti ih sa solidarnošću, društvenom podjelom ili određenim svjetonazorskim sukobom.

---

## Vlastite imenice i tematske veze

Treći dio prikazuje najčešće lematizirane vlastite imenice uz dominantne teme i korelacije supojavljivanja tematskih oznaka. Automatsko izdvajanje ne utvrđuje ulogu imenovanog aktera ni postojanje širega narativnog okvira.

### Najčešće vlastite imenice prema tematskim kategorijama

Grafikon prikazuje najčešće lematizirane vlastite imenice uz najzastupljenije dominantne teme. Riječ je o automatski izdvojenim spominjanjima, pa oznaka ne mora uvijek predstavljati osobu ili instituciju, a često spominjanje ne znači da je akter nositelj narativa.

Poredak treba čitati kao kartu leksičke prisutnosti. On omogućuje provjeru razlikuju li se najčešća imena među temama, ali bez ručne validacije entiteta ne potvrđuje funkcionalne granice tematskih domena.

```{r}
#| label: plot-actors-in-topics
#| fig-height: 14
#| fig-width: 14

ggplot(top_actors_in_top_topics, aes(x = n, y = reorder_within(lemma, n, dominant_topic), fill = dominant_topic)) +
  geom_col(show.legend = FALSE, width = 0.8) +
  scale_y_reordered() +
  scale_fill_digikat() +
  facet_wrap(
    ~ dominant_topic,
    scales = "free_y",
    ncol = 1,
    labeller = labeller(dominant_topic = as_labeller(digikat_topic_label))
  ) +
  labs(
    title = "Najčešće vlastite imenice u najzastupljenijim tematskim kategorijama",
    subtitle = "Najčešći automatski izdvojeni lematizirani oblici po dominantnoj temi",
    x = "Ukupan broj spominjanja",
    y = "Akter (lematizirana forma)"
  ) +
  theme_digikat(base_size = 24) +
  theme(
    plot.title = element_text(face = "bold", size = 30, margin = margin(b = 5)),
    plot.subtitle = element_text(size = 20, color = dk_col$muted, margin = margin(b = 20)),
    strip.text = element_text(face = "bold", size = 18, hjust = 0),
    panel.grid.major.y = element_blank(),
    panel.grid.major.x = element_line(color = dk_col$grid),
    panel.grid.minor = element_blank(),
    plot.margin = margin(20, 20, 20, 20)
  )
```

### Mreža povezanosti tematskih kategorija

Mreža prikazuje korelacije supojavljivanja rječničkih oznaka unutar istih dokumenata. Prikazane su samo veze iznad praga 0,1. One opisuju povezanost oznaka, a ne uzročnu ili uređivačku vezu među temama.

Tri najjače korelacije u novom uzorku povezuju `r pair_labels`. Prostorni raspored mreže služi preglednosti i ne predstavlja potvrđene, odvojene klastere ako klasterski model nije posebno procijenjen.

```{r}
#| label: plot-topic-network
#| fig-height: 12
#| fig-width: 14

set.seed(123)
topic_pairs %>%
  filter(correlation > 0.1) %>%
  graph_from_data_frame() %>%
  ggraph(layout = "fr") +
  geom_edge_link(aes(edge_alpha = correlation, edge_width = correlation), color = dk_col$accent_200) +
  geom_node_point(color = dk_col$accent, size = 7) +
  geom_node_text(
    aes(label = str_wrap(digikat_topic_label(name), 20)),
    repel = TRUE,
    size = 6.5,
    color = dk_col$ink
  ) +
  theme_digikat_void(base_size = 24) +
  labs(
    title = "Mreža povezanosti tematskih kategorija",
    subtitle = "Koje se teme najčešće pojavljuju zajedno u istim člancima?",
    edge_width = "Snaga veze (korelacija)",
    edge_alpha = ""
  )
```

## Sinteza i zaključci

Analiza razlikuje `r nrow(engagement_by_topic)` rječnički definiranih tematskih kategorija te njihove frekvencije, intenzitet, zabilježene reakcije i supojavljivanja. Te mjere opisuju različite profile tema, ali ne utvrđuju same po sebi zasebne publike ili autonomne diskursne domene.

::: {.featured-box}
### Tematske struje u novom uzorku
Najzastupljenije su `r top_topic_names`; najveći prosjek interakcija imaju `r top_engagement_names`. Love i Angry reakcije daju dodatni, ali platformski ograničen signal recepcije.
:::

Podaci podupiru uži zaključak o preklapajućim tematskim strujama, ne strogu podjelu diskursa na dva svijeta. Pojedine teme razlikuju se po frekvenciji, gustoći rječničkih podudaranja i zabilježenim reakcijama, dok mreža pokazuje da se oznake povezuju preko više kombinacija.

Mreža dodaje relacijsku dimenziju: pokazuje koje rječničke oznake češće supostoje u istim dokumentima. Za tumačenje pojedinačnih veza kao političke instrumentalizacije, privatne duhovnosti ili javnoga identiteta trebalo bi pregledati pripadajuće tekstove, što ova agregirana stranica ne čini.

Sljedeća mapa, [Atmosfera diskursa](diskurs.html), istom uzorkovnom logikom analizira leksički tonalitet, emocije i konfliktni rječnik. Ni ona iz leksičkih pokazatelja ne izvodi komunikacijske namjere bez dodatne validacije.
